# Phase 1 symbol baseline for build/lib/libpakka.a — frozen snapshot.
#
# DO NOT REGENERATE. This file captures the defined-globals surface of
# the static library at the END of Phase 1 of the libpakka migration,
# before any renames or function moves. Running `make symbol-audit`
# today would overwrite it with the post-migration 39-`pakka_*`
# surface and lose the comparison point.
#
# The 34 names below are all unprefixed (no `pakka_` namespace yet);
# Phases 2-6 either renamed them to `pakka_*`, deleted them outright
# (debug.c, the legacy CLI wrappers), or moved them out of the
# library entirely (pakka_die / pakka_die_e / pakka_fprint_sanitized /
# pakka_sanitize_name went to src/main.c).
#
# `make symbol-audit` is now a hard gate that fails the build if any
# non-`pakka_` symbol appears in build/lib/libpakka.a. Run it against
# the current build to see the post-migration list; diff against this
# file to see what changed.
add_file
add_files
add_folder
add_to_pak
close_pakfile
compat_dirname
compat_getcwd
compat_is_reparse_or_symlink
compat_lstat
compat_mkdir
compat_mkstemp_open
compat_open_extract_target
compat_realpath
compat_rename_noreplace
compat_rename_replace
compat_strdup
compat_try_exclusive_lock
create_pakfile
debug_directory_entry
debug_header
delete_files
error_exit
error_exit_e
extract_files
file_exists
filesize
fprint_sanitized
list_files
list_files_tree
mkdir_r
open_pakfile
read_u32_le
sanitize_name
write_u32_le
